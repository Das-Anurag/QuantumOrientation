document.body.innerHTML += "<p>main.js is working!</p>";        camera.updateProjectionMatrix();

        renderer.setSize(
            window.innerWidth,
            window.innerHeight
        );

    }
);
